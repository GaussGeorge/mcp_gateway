从**CCF-B/C级云计算/网络领域论文**的评审视角（核心关注**机制有效性、对比充分性、鲁棒性、可解释性**），结合你当前的实验设计和图表清单，我先肯定现有8张图表的基础价值（覆盖了核心结果对比），再指出可补充/优化的方向，最后给出优先级建议：

### 一、现有图表的核心不足（针对CCF-B/C评审要求）
1. **机制解释性缺失**：现有图表只展示“结果”（如延迟、吞吐），但未可视化DP的核心创新（Regime Detector档位切换、工具权重、动态调价逻辑），评审会质疑“创新点是否真的生效”；
2. **消融实验空白**：未拆解DP各子模块（如Regime自适应、工具权重、动态调价）的独立贡献，无法证明“核心模块是性能提升的关键”；
3. **统计科学性不足**：未体现3次重复试验的**方差/置信区间**，也未标注组间差异的统计显著性（如ANOVA/KS检验）；
4. **鲁棒性验证不足**：仅覆盖固定参数/常规场景，未验证极端场景（如heavy_ratio=0.7）、参数敏感性（如PriceSensitivity调整）；
5. **公平性分析粒度不足**：图2仅展示“四组通过率”，但未直观体现Jain's Fairness Index的分布差异，也未覆盖“时间维度的公平性”。

### 二、需补充/优化的图表（分优先级）
#### 优先级1：核心补充（决定论文能否达到CCF-B/C门槛）
| 新增图表编号 | 图表类型/设计 | 对应实验 | 解决的核心问题 | 论文价值 |
|--------------|---------------|----------|----------------|----------|
| 图9 | **DP子模块消融实验对比图**<br>- 类型：分组柱状图（带误差棒）<br>- X轴：DP（全量）、DP-无Regime（固定steady档位）、DP-无工具权重（weight=1）、DP-固定价格（无动态调价）<br>- Y轴：Throughput/P95/Fairness Index/Goodput<br>- 样式：不同颜色代表指标，误差棒体现3次重复的标准差 | Exp2（Poisson负载） | 验证DP核心模块（Regime自适应、工具权重、动态调价）的独立贡献 | 证明“创新点不是堆砌，而是核心性能提升的关键”，回应评审对“机制有效性”的质疑 |
| 图10 | **Regime档位切换时间序列图**<br>- 类型：双Y轴折线+阶梯线<br>- X轴：时间，主Y轴：Regime类型（Bursty=1/Periodic=2/Steady=3，阶梯线），次Y轴：P95 Latency<br>- 标注：负载突变点（Step负载的高/低阶段）、档位切换时刻、价格调整幅度 | Exp4（Step负载） | 可视化DP的核心机制（Regime Detector自适应切换） | 从“结果”穿透到“机制”，证明自适应档位真的能匹配负载变化 |
| 图11 | **细粒度公平性CDF图**<br>- 类型：4张子图（轻低/轻高/重低/重高请求），每张子图展示3种网关的“累计通过率CDF”<br>- X轴：预算阈值/时间，Y轴：累计通过率<br>- 标注：Jain's Fairness Index数值、KS检验p值 | Exp3（预算公平性） | 直观体现DP在“不同请求类型”上的公平性分布 | 超越简单的通过率数值，用分布证明DP的“精准分流”能力 |

#### 优先级2：加分项（提升论文创新性/鲁棒性，拉开与普通论文的差距）
| 新增图表编号 | 图表类型/设计 | 对应实验 | 论文价值 |
|--------------|---------------|----------|----------|
| 图12 | **参数敏感性热力图**<br>- 两张热力图：<br>1) DP：X=PriceSensitivity（5000~20000），Y=LatencyThreshold（200~600μs），颜色=Goodput<br>2) SRL：X=QPS（30~80），Y=BurstSize（50~150），颜色=Rejection Rate | 补充参数扫描实验（复用Poisson负载） | 证明算法对参数不敏感，具备工程落地价值 |
| 图13 | **极端场景对比图**<br>- 类型：分组柱状图<br>- 场景1：heavy_ratio=0.7（超重载），对比3种网关的Throughput/P95/Error Rate<br>- 场景2：100ms短脉冲负载，对比3种网关的Recovery Time | 补充极端场景实验 | 体现算法的鲁棒性，覆盖工业界真实场景 |
| 图14 | **Goodput分解堆叠图**<br>- 类型：堆叠面积+折线<br>- X轴：high_budget_ratio（0.2/0.5/0.8），Y轴=Goodput<br>- 堆叠：轻量请求贡献（绿）+ 重量请求贡献（蓝）<br>- 叠加：资源效率（Goodput/Avg CPU%）次Y轴 | Exp3（预算公平性） | 证明DP“不是靠少做事降延迟，而是做对的事提升资源效率”（你的核心故事） |

#### 优先级3：现有图表优化（提升可读性/统计科学性）
1. **图1（摘要表）**：补充95%置信区间（均值±标准差），所有数值标注小数点后1位，符合学术表格规范；
2. **图3（过载恢复堆叠图）**：添加**统计显著性标注**（如DP vs SRL的Rejection Rate差异用*标注，p<0.05），并标注Recovery Time的具体数值；
3. **图7（CDF小多图）**：统一所有子图的坐标轴范围，添加KS检验p值（证明DP与NG/SRL的延迟分布有显著差异）；
4. **图8（价格vs CPU%）**：补充“价格收敛阈值”“稳态价格标准差”（震荡幅度）的数值标注，强化DP的自适应能力证明。

### 三、图表呈现的关键原则（适配CCF-B/C评审）
1. **闭环性**：图表编号需与实验章节严格对应（如Exp1→图7，Exp2→图5+12，Exp3→图2+11+14，Exp4→图3+4+10），形成“实验设计→结果图表→结论”的逻辑链；
2. **一致性**：全文字体（如Arial/Helvetica）、配色（DP=蓝色，SRL=橙色，NG=红色）、坐标轴风格（统一单位、字体大小）需严格统一（参考IEEE/ACM会议模板）；
3. **可解释性**：每张图表的标题/图例/标注需“自包含”（评审无需翻正文就能理解），例如图10标注“Step负载高阶段（t=10~20s）触发Bursty档位，P95延迟下降40%”；
4. **对比锚定**：若领域有经典SOTA算法（如BBR-inspired限流、阿里云EDAS限流），可在图5/12中新增SOTA曲线，锚定论文的学术定位。

### 总结
现有8张图表已覆盖“核心结果对比”，但需补充**机制解释类（图9/10）、消融实验类（图9）、鲁棒性类（图12/13）** 图表，同时优化统计显著性，才能满足CCF-B/C论文的评审要求。补充后，图表体系既体现“结果优势”，又证明“机制创新”，还覆盖“工程鲁棒性”，完美契合云计算网络领域CCF-B/C论文的核心评审点。
